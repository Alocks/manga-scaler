name: Build Extension CRX

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Check for new commits
        id: commit_check
        run: |
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LATEST_TAG" ]; then
            echo "No release tag found; continuing."
            echo "should_build=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          COMMITS_SINCE_TAG=$(git rev-list --count "$LATEST_TAG..HEAD")

          if [ "$COMMITS_SINCE_TAG" -eq 0 ]; then
            echo "No new commits since $LATEST_TAG; skipping build."
            echo "should_build=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Found $COMMITS_SINCE_TAG commit(s) since $LATEST_TAG; continuing build."
          echo "should_build=true" >> "$GITHUB_OUTPUT"

      - name: Setup Node.js
        if: steps.commit_check.outputs.should_build == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Get anime4K-webgpu package
        if: steps.commit_check.outputs.should_build == 'true'
        run: npm install anime4k-webgpu

      - name: Build runtime bundle.js
        if: steps.commit_check.outputs.should_build == 'true'
        run: node tools/build-runtime-bundle.mjs

      - name: Build Anime4K.js and prepare minimal extension payload
        if: steps.commit_check.outputs.should_build == 'true'
        run: |
          git clone https://github.com/monyone/Anime4K.js temp_anime4k
          cd temp_anime4k && yarn install && yarn build
          cd ..
          mkdir -p extension_root/src/runtime
          cp manifest.json extension_root/
          cp content.js extension_root/
          cp popup.html extension_root/
          cp popup.js extension_root/
          cp rules.json extension_root/
          cp LICENSE extension_root/
          cp src/runtime/runtime.bundle.js extension_root/src/runtime/runtime.bundle.js

          mkdir -p extension_root/node_modules/anime4k-webgpu/lib
          cp -R node_modules/anime4k-webgpu/lib/. extension_root/node_modules/anime4k-webgpu/lib/

          mkdir -p extension_root/node_modules/anime4k-webgl
          cp temp_anime4k/dist/anime4k.js extension_root/node_modules/anime4k-webgl/anime4k.js
          rm -rf temp_anime4k

      - name: Verify payload only contains required files
        if: steps.commit_check.outputs.should_build == 'true'
        run: |
          test -f extension_root/manifest.json
          test -f extension_root/LICENSE
          test -f extension_root/content.js
          test -f extension_root/popup.html
          test -f extension_root/popup.js
          test -f extension_root/rules.json
          test -f extension_root/src/runtime/runtime.bundle.js
          test -f extension_root/node_modules/anime4k-webgpu/lib/index.js
          test -f extension_root/node_modules/anime4k-webgl/anime4k.js
          test ! -f extension_root/node_modules/anime4k-webgpu/package.json
          test ! -f extension_root/node_modules/anime4k-webgpu/README.md

      - name: Build release ZIP artifact
        if: steps.commit_check.outputs.should_build == 'true'
        run: |
          cd extension_root
          zip -r ../manga-scaler.zip .
          cd ..
          test -f manga-scaler.zip

      - name: Install Chrome
        if: steps.commit_check.outputs.should_build == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: Pack Extension (CRX)
        if: steps.commit_check.outputs.should_build == 'true'
        env:
          EXTENSION_KEY: ${{ secrets.PEM }}
        run: |
          echo "$EXTENSION_KEY" > key.pem
          google-chrome --headless --pack-extension=extension_root --pack-extension-key=key.pem
          mv extension_root.crx manga-scaler.crx

      - name: Generate release notes
        if: steps.commit_check.outputs.should_build == 'true'
        id: release_notes
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          if [ -n "$PREV_TAG" ]; then
            COMMITS=$(git log "$PREV_TAG..HEAD" --pretty=format:"- %s" --no-merges)
          else
            COMMITS=$(git log --pretty=format:"- %s" --no-merges)
          fi
          echo "commits<<EOF" >> $GITHUB_OUTPUT
          echo "$COMMITS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        if: steps.commit_check.outputs.should_build == 'true' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: "v1.0.${{ github.run_number }}"
          files: |
            manga-scaler.zip
            manga-scaler.crx
          body: |
            Build #${{ github.run_number }}

            ${{ steps.release_notes.outputs.commits }}
          draft: false
          prerelease: false
